services:
  # Go Backend API
  - type: web
    name: habit-tracker
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: APP_ENV
        value: production
      - key: PORT
        value: 8080
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: SUPABASE_JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health
    
  # PostgreSQL Database (if not using Supabase)
  # - type: pserv
  #   name: habittracker-db
  #   env: docker
  #   plan: free
  #   region: oregon
  #   dockerfilePath: ./Dockerfile.postgres
  
  # Redis Cache (if not using Supabase)
  # - type: redis
  #   name: habittracker-redis
  #   plan: free
  #   region: oregon
